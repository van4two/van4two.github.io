{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Jekyll Serve (with livereload)",
            "type": "shell",
            "command": "sudo bundle exec jekyll serve --livereload --incremental",
            "group": {
                "kind": "build",
                //"isDefault": true
            },
            //"isBackground": true,
        }
    ]
}