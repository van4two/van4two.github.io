name: Testing the GitHub Pages publication

on:
  push:
    branches: main
    
  workflow_dispatch:
  
jobs:
  jekyll:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2          
